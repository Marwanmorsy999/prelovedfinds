{
  "install": "bun install",
  "dev": "bun run dev",
  "build": "bun run build",
  "preview": "bun run preview",
  "setup": [
    "bun install",
    "bunx wrangler d1 migrations apply prelovedfinds-db --local"
  ]
}
